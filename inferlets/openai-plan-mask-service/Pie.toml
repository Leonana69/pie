[package]
name = "openai-plan-mask-service"
version = "0.1.0"
description = "OpenAI-compatible chat completion service with cached catalogs, prior plans and example masks"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

[parameters]
